import {
  REGISTRO_EXITOSO,
  REGISTRO_ERROR,
  OBTENER_USUARIO,
  LOGIN_EXITOSO,
  LOGIN_ERROR,
  CERRAR_SESION,
} from "../../types/Index";

const alertaReducer = (state, action) => {
  switch (action.type) {
    default:
      break;
  }
};

export default alertaReducer;
